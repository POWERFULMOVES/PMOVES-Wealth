version: "3.9"
services:
  firefly:
    image: ghcr.io/POWERFULMOVES/pmoves-firefly-iii:main
    restart: unless-stopped
    networks: [pmoves-net]
    ports: ["${FIREFLY_PORT:-8075}:8080"]
networks:
  pmoves-net:
    external: true

